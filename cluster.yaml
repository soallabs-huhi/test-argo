apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: poc-eks
  region: eu-central-1
  version: "1.34"

managedNodeGroups:
  - name: ng-main
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 3
    volumeSize: 50
